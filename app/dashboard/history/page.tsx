import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'History',
};

export default async function HistoryPage() {
  return (
    <div>
      <h1>History Page</h1>
    </div>
  );
}
