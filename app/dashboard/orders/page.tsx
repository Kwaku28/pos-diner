import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Orders',
};

export default async function OrdersPage() {
  return (
    <div>
      <h1>Orders Page</h1>
    </div>
  );
}
